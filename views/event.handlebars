{{#if event_name}}
<div>
  <h4 class="font-weight-bold title p-3">Your Friend's DateRescue Page</h4>
  <div class="p-3">
    <p><span class="font-weight-bold">Date:</span>
      {{event_date}}
    </p>
    <p><span class="font-weight-bold">Time:</span> {{event_time}}</p>
    <p><span class="font-weight-bold">Location:</span> {{event_location}}</p>
    <iframe width="300" height="200" frameBorder="0"
      src='https://www.google.com/maps/embed/v1/place?key={{API_KEY}}&q={{event_location}}' 
      allowFullScreen></iframe>
    <p><span class="font-weight-bold">Note:</span> {{event_note}}</p>
    <p><img width="300px" src={{event_date_picture}}></img></p>
  </div>
</div>
{{else}}
<div class="m-4">
  <h4>Sorry, this event address is either invalid or has expired.</h4>
  <p>If you think there's been some sort of mistake, ask your friend to resend their unique event address,
    or if this is your event, login to <a href="/">Date Rescue</a> and double-check the address is correct.</p>
</div>